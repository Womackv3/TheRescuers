extends Control

func _on_continue_pressed() -> void:
    # Validate next stage via GameManager
    # Note: Logic to load next level implies GameManager state update
    # Here we assume the scene change happens after button press
    pass
